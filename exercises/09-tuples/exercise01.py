# Exercise 1: Variable Swap and Basic Unpacking
# Swap two variables without a temp variable using tuple unpacking.
# Then unpack a tuple, a list, and a string into individual variables.

a, b = 5, 10
print(f"Before swap: a={a}, b={b}")

# Swap using tuple unpacking


print(f"After swap: a={a}, b={b}")

# Unpack a tuple
data = ("Python", 3.14, 1991)


print(f"language={language}, version={version}, year={year}")

# Unpack a list
coords = [100, 200, 300]


print(f"From list: x={x}, y={y}, z={z}")

# Unpack a string
letters = "XYZ"


print(f"From string: c1={c1}, c2={c2}, c3={c3}")
