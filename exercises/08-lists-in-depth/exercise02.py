# Exercise 2: Number Sorter
# Hardcode a list of numbers. Sort them with .sort() and sorted() to see the
# difference. Then sort a list of strings by length using key=len. Finally,
# sort a list of tuples by the second element using key=lambda x: x[1].
