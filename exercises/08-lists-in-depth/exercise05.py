# Exercise 5: Matrix Transposer
# Hardcode a 3x4 matrix as a list of lists. Use a nested list comprehension to
# transpose it (rows become columns). Also implement the same with zip(*matrix).
# Print both the original and transposed matrices in a grid format.
