# Exercise 7: Unique & Sort
# Hardcode a list with duplicates. Remove duplicates (using set or manual loop),
# sort in descending order, filter out numbers below a threshold, and find the
# index of a specific value using .index(). Print each step.
