# Exercise 1: Shopping List Builder
# Start with an empty list. Keep asking the user to add items (append), insert
# at a specific position (insert), remove items (remove), and pop the last item.
# Show the list after each operation. Type "done" to finish.
